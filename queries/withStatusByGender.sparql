#ooa number of oath swearers with status by gender
prefix t: <http://www.tei-c.org/ns/1.0/>
prefix ooa: <http://gams.uni-graz.at/o:ooa.ontology/#>
SELECT ?gender (COUNT(?s) AS ?count) WHERE {
	?s a ooa:OathSwearer;
        t:socecStatus ?status;
        t:sex ?gender.
}
GROUP BY ?gender